"""
Steps to run the tests
1-Clone the repo
2-install the requirements file using command "pip install -r requirements.txt"
3-Open a terminal on the IDE ,change the directory to "../husby" run the server using command "python main.py"
4-Open another terminal on the IDE ,change the directory to "../Automation" , then run the tests using command "robot Tests.robot"
"""